{
  "extends": "@tsconfig/node22/tsconfig.json",
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "moduleResolution": "node",
    "outDir": "./dist",
    "rootDir": "./",
    "baseUrl": ".", // project root
    "paths": {
      "#*": ["src/*"] // matches the import‑map
    }
  },
  "include": ["**/*.ts"],
  "exclude": ["dist"]
}
